## 1. App State & Layout Integration

- [x] 1.1 Add `sidebarOpen` state and toggle functions inside `App.tsx`
- [x] 1.2 Implement the mobile top header with a hamburger menu button inside the main view container in `App.tsx`

## 2. Sidebar Integration

- [x] 2.1 Add the close button (X symbol) inside `Sidebar.tsx` and hook up close callbacks
- [x] 2.2 Automatically trigger closing of the mobile sidebar when any sidebar menu item is clicked in `Sidebar.tsx`

## 3. Styling & Verification

- [x] 3.1 Write responsive CSS styles in `App.css` to position and animate the sidebar and backdrop drawer overlay on mobile screens
- [x] 3.2 Verify compile/build and manual checks of layout on mobile viewports
