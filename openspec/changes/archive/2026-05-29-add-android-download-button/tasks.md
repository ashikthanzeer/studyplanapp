## 1. Auth Page Integration

- [x] 1.1 Add the Android download button structure and inline SVG icon inside `Auth.tsx` below the authentication card
- [x] 1.2 Implement the external link click handler to open the Expo build URL in a new tab

## 2. Sidebar Integration

- [x] 2.1 Add the Android download button structure and inline SVG icon in `Sidebar.tsx` positioned above the profile block
- [x] 2.2 Implement the external link click handler to open the Expo build URL in a new tab

## 3. Styling & Verification

- [x] 3.1 Add layout and hover styles in `App.css` or `index.css` to match the glassmorphism theme of the app
- [x] 3.2 Verify the buttons display correctly on both the authenticated app view (desktop & mobile sidebar) and unauthenticated login screen
